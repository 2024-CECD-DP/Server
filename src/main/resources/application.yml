spring:
  datasource:
    url : jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: Wngurdl1!

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate.format_sql: true
    show-sql: true
